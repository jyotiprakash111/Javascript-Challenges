function sortStrings(arr) {
    return arr.sort();
  }
  
  // Example usage:
  const strings = ["date", "elderberry","biaaa","expensive", "apple", "banana", "cherry", "grapes"];
  console.log(sortStrings(strings)); // Output: ['apple', 'banana', 'cherry', 'date', 'elderberry']
  